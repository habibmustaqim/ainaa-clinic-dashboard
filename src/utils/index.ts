/**
 * Utility Functions
 * Reusable formatting and helper utilities
 */

export {
  formatCurrency,
  formatDate,
  formatPhone,
  formatIcNumber,
  formatNumber,
  formatPercentage,
  getInitials,
  truncateText
} from './formatters'

export type { DateFormat } from './formatters'
